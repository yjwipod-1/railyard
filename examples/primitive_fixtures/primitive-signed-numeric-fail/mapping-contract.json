{
  "contract_id": "primitive-signed-num-fail",
  "version": "0.7.2",
  "applies_to": "primitive-fixture",
  "validation_scope": "transform_only",
  "missing_mapping_policy": "inconclusive",
  "field_mappings": [
    {"source_path": "record_id", "derived_path": "record_id", "transform": "identity", "required": true},
    {"source_path": "signed_amount", "derived_path": "signed_amount", "transform": "identity", "required": true, "preserve_sign": true}
  ]
}
