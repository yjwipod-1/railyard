interface:
  display_name: "Railyard"
  short_description: "Run a reusable epic-ticket workflow with SQLite control tables, Architect dispatch, Runner prompts, and review gates."
  default_prompt: "Use $railyard to route work across lanes with epics, tickets, helper-backed SQLite access, Architect dispatch, Runner execution, and explicit review."

policy:
  allow_implicit_invocation: true
